function getW(w) {
	return w / 640 * $(window).width();
}

function getH(h) {
	return h / 1008 * $(window).height();
}

var pages = {};

var showPageFunction = {
	1 : null,
	2 : null,
	3 : null,
	4 : null,
};

(function() {
	var ps = $(".J_page[data-pageId]");
	for (var i = 0; i < ps.length; i++) {
		var p = $(ps[i]);
		pages[p.attr("data-pageId")] = p;
	}
})();

function goPage(i) {
	if (i in pages) {
		$(".J_page").removeClass("A_show").css({
			"z-index" : 2
		})
		var j = i - 1;
		if (j in pages) {
			pages[j].css({
				"opacity" : 1
			}).addClass("A_out");
		}
		pages[i].